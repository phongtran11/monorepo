# Environment
NODE_ENV=development

# Backend API base URL 
API_URL=http://localhost:8000/api/v1

# Log level: trace | debug | info | warn | error | fatal (optional, defaults to debug in dev, info in prod)
# LOG_LEVEL=debug
